{
  "name": "ordnung-site",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf out && powderworks-docs build content --out out --site-url https://ordnung.powderworks.dev --name Ordnung --description \"Repository order, made explicit.\" --github PowderworksCode/ordnung --license MIT --logo /icon.svg --static public",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "bun test"
  },
  "dependencies": {
    "powderworks-docs": "github:PowderworksCode/docs"
  },
  "devDependencies": {
    "wrangler": "^4.123.0"
  }
}
