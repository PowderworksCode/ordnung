ORDNUNG / POWDERWORKS FLEET — HANDOFF NOTES
Written 2026-08-02, before moving machines.
Updated 2026-08-05: workflows landed, Dependabot distributed.


== 0. CURRENT STATE ==

The nine uncommitted files this section used to warn about all landed in
3d494d1. Nothing is stranded.

Every member now has CI. One `gate` job per repository runs
`cargo fmt --all --check`, `cargo clippy --workspace --all-targets --
-D warnings`, and `cargo test --workspace`, on push, pull request, and a
weekly schedule. Actions are pinned by commit SHA. The toolchain comes
from each repository's rust-toolchain.toml — 1.97.1 fleet-wide, matching
in fact — rather than from a setup action.

Straitjacket, in fact, and cowbird build Entl from a sibling checkout, so
their workflows check the sibling repositories out alongside. Those
checkouts are unpinned and track default branches, which is a real
coupling: an Entl change can break their CI with nothing landing in
them. The weekly schedule exists partly to catch that.

Dependabot is fleet-owned as of conf#1, distributed as one glob-driven
file to every member rather than a file per repository shape.

Required findings across the seven members: 22 -> 11. Straitjacket and
cowbird are at zero. Nothing in the Rust half of the fleet fails
anything. The 11 that remain are three questions:

  - conf and website have no workflow yet          (4 findings)
  - ordnung/site and website/site have no lint,
    format, or test tasks                          (5 findings)
  - two JavaScript helper directories: entl's
    providers/typescript and infact's
    tools/ts-scoreboard                            (2 findings)


== 1. OPEN QUESTIONS AWAITING A DECISION ==

1.1 Action reference before v0.1.0.
    A member workflow must reference the Action somehow. `@main` is a
    mutable ref, which Ordnung's own `pinned-actions` check rejects as
    required. Chicken-and-egg with the release. Leaning: tag an early
    0.1.0-rc so workflows can pin a real revision.

1.2 test-mirror: 262 of 354 local findings (74%).
    Requiring one test file per source file is the single biggest item
    on the board. It is allow_override = true, so members can carry a
    documented exception while the backlog is worked down. Decision
    needed: work through it, grant exceptions, or reconsider the rule.
    Note test-inline (the "no inline #[cfg(test)]" half) is only 35
    findings and is the part originally asked for.

1.3 Promote website/.githooks into conf as managed content?
    website/.githooks is the only real hooks implementation in the
    fleet (commit-msg enforcing Conventional Commits, pre-commit
    running the gate, run-straitjacket helper). Distributing it via
    conf would fix six of the seven git-hooks failures at once. Not
    done: hook contents are a content decision, and run-straitjacket
    assumes straitjacket is on PATH.

1.4 Vale and Stylelint configs.
    paranoid requires both. No .vale.ini anywhere except website; no
    Stylelint config anywhere (ordnung/site fails it). Needs a prose
    style decision before conf can distribute them as managed files.

1.5 Graduation preview.
    Offered, not approved. `checks_for` already takes a stage, so a
    `--as-stage supported` flag on check/repo-check would answer "what
    would this repo fail if promoted today?". Roughly an hour. Matters
    because incubating is explicitly temporary and nothing currently
    signals when a repo is ready.

1.6 straitjacket's stage.
    Marked incubating along with everything else, per "everything is
    incubating at this point". Confirm that is intended.


== 2. DECIDED — DO NOT RELITIGATE ==

- paranoid is the target; incubating is temporary scaffolding.
- All 7 members are incubating today. Graduating one is a deliberate
  edit to conf/.ordnung/fleet.toml.
- Stage is fleet-assigned, never self-declared by a member.
- Visibility, as of 2026-08-05: entl and in fact are public; ordnung,
  straitjacket, cowbird, website, and conf are private. This line has
  drifted before, so check it rather than trusting it.
  Ordnung going public is a v0.1.0 prerequisite, not a preference: the
  README documents the shipped tiers as consumed through
  `[[extends]] git = "https://github.com/PowderworksCode/ordnung"`, and
  a member workflow referencing the Action needs the same. Neither works
  against a private repository, for third parties or for conf.
- Managed-entry override is name-keyed only. Cross-layer collisions
  between differently named entries stay an error.
- present / absent / unmanaged are three distinct states.
- Downstream layers may override anything they inherit; allow_override
  governs member repositories only.
- Config lives in .ordnung/ ; the directory is the unit of publication.
- Built-in defaults are the industry floor (17 required / 21
  recommended / 7 off). Tool mandates and house conventions belong in
  a tier, not in defaults.
- Rust required dependency: itertools. TypeScript: remeda, ts-pattern,
  zod.
- repo-check does NOT take --fleet. Member workflows run the fast gate
  at built-in defaults; conf runs the fleet-aware audit. Reason: conf
  is private, so --fleet would put a cross-repository token into member
  repositories just to perform a read. If member-side fleet awareness
  is needed later, distribute a fleet-owned policy snapshot as managed
  content rather than fetching conf.


== 3. RELEASE PLAN STATUS ==

  1. Publish the Entl boundary ................ DONE
     entl e40d011 pushed; ordnung pins it; verified from a fresh clone
     with --locked. entl HEAD has since moved to 16f0c21; the pin is
     deliberately behind and ordnung still builds against it.

  2. Initialize the fleet configuration repo .. DONE
     conf created, renamed from fleet-configuration, private, pushed.
     Org rename ThePowderworks -> PowderworksCode complete everywhere.

  3. Workflows ................................ DONE (2026-08-04)
     Five code repositories gained a gate; see section 0. They were
     enabled rather than started disabled, because four of the five
     passed the full gate locally before the workflow landed.
     Not done: conf and website still have no workflow. Website's three
     remaining findings close together whenever it gets one.

  4. FLEET_GITHUB_TOKEN ....................... NOT STARTED
     Needs cross-repo read, branch write, PR write. Only conf needs it.
     Note the interaction with ordnung's visibility: if ordnung is
     public, conf resolves its inherited policy through a pinned git
     extends with no token at all, and the token is then needed only for
     the scheduled fleet-sync-all rather than for reading policy.

  5. Dogfood and resolve findings ............. IN PROGRESS
     22 required findings -> 11. Dependabot (6) closed by conf#1.
     TypeScript fixture findings closed by ignoring **/tests/fixtures/**
     in entl and in fact — those fixtures were being read as projects
     owing CI tasks, a type layer, and a managed Biome config.
     The 11 that remain are listed in section 0.
     Unchanged: test-mirror still dominates the off-severity findings,
     and the ~24 findings deferred by incubating still return on
     graduation.

  6. Expand automatic fixes ................... NOT STARTED
     `ordnung fix` reports no automatic remediations for every member.
     Best first candidate is dependabot.yml: the check already derives
     every lockfile owner, so it could generate an exact file per
     repository and retire both the glob and the managed entry.

  7. Release 0.1.0 ............................ NOT STARTED
     Version is still 0.0.0 across the workspace. Ordnung going public
     is a prerequisite — see section 2.

  Suggested order: publish ordnung -> conf workflow -> token ->
  branch protection on ordnung -> graduate ordnung -> repeat.


== 4. KNOWN GAPS AND SHARP EDGES ==

- CheckScope is declared on all 47 checks but is not yet consumed by
  policy. It is groundwork for directory-scoped rules. Without it a
  path-scoped rule aimed at a repository-scoped check such as
  branch-protection would silently do nothing.

- Directory-level policy is designed but not built. If it happens:
  prefer the existing project selector (language / ecosystem /
  capability) over raw path globs, since paths are brittle under the
  churn monorepos actually see. Add `path` only as an escape hatch.
  Do NOT add cascading per-directory config files.

- Check configuration does not travel through fleet policy, only
  severities do. TestLayoutConfig lives in RepoConfig, so its ignore
  list and per-language roots are per-repository. This is why
  scan_inline / require_mirror became two checks rather than staying
  two booleans.

- Some GitHub-backed checks cannot reach a verdict on every plan and
  visibility combination. Where the API refuses or the setting is not
  offered, the check skips with the reason rather than guessing, so it
  does not gate. Which ones apply depends on the org plan at the time.

- ruleset-bypass only passes when a gating ruleset EXISTS and has a
  bypass actor. With no rulesets it skips forever. Creating them was
  deliberately deferred until workflows exist, so required status
  checks can be designed at the same time.

- Demoting a check also demotes its errors. An unreadable GitHub
  setting on an advisory check no longer makes a report unclean. This
  is intended; it is documented in design.md.

- Managed entry names are the public interface of a published
  configuration. Renaming one is a breaking change for importers.

- The Git fetch path for extends requires the server to allow fetching
  an arbitrary SHA (uploadpack.allowAnySHA1InWant). GitHub allows it;
  a default-configured self-hosted Git server would reject a pinned
  fetch. Relevant if outside parties publish confs.

- Cargo cannot fetch git dependencies on this machine without
  CARGO_NET_GIT_FETCH_WITH_CLI=true, because a global insteadOf rule
  rewrites https to ssh and ssh-agent holds no identities. Machine
  quirk, not a repo problem — deliberately not committed as
  .cargo/config.toml. May or may not apply on the new machine.

- Ordnung fails its own required-dependencies: itertools missing from
  both crates, remeda and ts-pattern missing from site. Adding four
  currently-unused dependencies is a deliberate call, not yet made.

- website/.githooks is committed and executable but scripts/dev.sh
  never sets core.hooksPath, so a fresh clone of website runs no
  hooks. The local working copy has it set by hand. Two-line fix.
  This is the finding that validated the new git-hooks check.


== 5. LOCAL DEVELOPMENT REMINDERS ==

- Use ./scripts/local_dev for cargo commands while a sibling ../entl
  checkout exists; it patches in the local Entl without touching the
  committed lockfile. Plain cargo uses the pinned revision.
- Full gate: ./scripts/local_dev test --workspace
             ./scripts/local_dev clippy --workspace --all-targets -- -D warnings
             ./scripts/local_dev fmt --check
- Fleet checks: ./target/debug/ordnung fleet check ../conf/.ordnung/fleet.toml
- in fact and cowbird are now fleet members and have remotes.
